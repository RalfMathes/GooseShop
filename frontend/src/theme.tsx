import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fb8b17",
      contrastText: "#fff",
    },
    secondary: {
      main: "#ff7500",
      contrastText: "#fff",
    },
    background: {
      default: "#FCEBCE",
    },
  },
});

export default theme;
