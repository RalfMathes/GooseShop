import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fb8b17",
      contrastText: "#fff",
    },
    secondary: {
      main: "#ec3a3a",
      contrastText: "#fff",
    },
    background: {
      default: "#fff7ee",
    },
  },
});

export default theme;
